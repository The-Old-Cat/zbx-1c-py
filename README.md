# zbx-1c-py

Инструмент для интеграции 1С с Zabbix, позволяющий собирать информацию о базах данных 1С через сервер администрирования кластера (RAC).

---

## 🚀 Возможности шаблона

- Структура проекта по лучшим практикам (`src/` layout)
- Управление зависимостями через **uv**
- Сборка через **hatchling**
- Группы зависимостей (`dev`, `security`)
- Настроенный `pytest`
- Логирование через `loguru`
- Проверка уязвимостей через `pip-audit`
- Готовые каталоги `data/` и `logs/` с `.gitkeep`
- Чистый и минималистичный `pyproject.toml`

---

## 📁 Структура проекта

```
project/
│
├── src/
│   └── zbx_1c_py/
│       ├── __init__.py
│       └── main.py
│
├── tests/
│   └── test_basic.py
│
├── data/
│   └── .gitkeep
│
├── logs/
│   └── .gitkeep
│
├── pyproject.toml
├── README.md
└── uv.lock
```

---

## 🛠 Установка зависимостей

```bash
uv sync
```

---

## ⚙️ Конфигурация

Для работы приложения требуется настроить следующие параметры:

- `1C_CLUSTER_ID` — ID кластера 1С (обязательный параметр)
- `RAC_PATH` — путь к утилите rac (по умолчанию: "rac")
- `RAC_HOST` — хост сервера администрирования 1С (по умолчанию: "127.0.0.1")
- `RAC_PORT` — порт сервера администрирования 1С (по умолчанию: 1545)
- `DEBUG` — режим отладки (по умолчанию: false)

Можно использовать файл `.env` в корне проекта для настройки этих параметров.
Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

Затем отредактируйте файл `.env`, указав свои значения:

```env
1C_CLUSTER_ID=your_actual_cluster_id
RAC_PATH=rac
RAC_HOST=127.0.0.1
RAC_PORT=1545
DEBUG=false
```

## ▶️ Запуск проекта

```bash
uv run src/zbx_1c_py/main.py
```

Для проверки конфигурации можно использовать вспомогательный скрипт:

```bash
uv run scripts/check_config.py
```

---

## 🧪 Запуск тестов

```bash
uv run pytest
```

---

## 🔐 Проверка уязвимостей

```bash
uv run pip-audit
```

---

## 📦 Сборка wheel‑пакета

```bash
uv build
```

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.  
См. файл `LICENSE`.
