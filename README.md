# Project-name

Современный шаблон Python‑проекта, основанный на `uv`, `hatchling` и структуре `src/`.  
Подходит для CLI‑утилит, сервисов, агентов и любых приложений на Python 3.12+.

---

## 🚀 Возможности шаблона

- Структура проекта по лучшим практикам (`src/` layout)
- Управление зависимостями через **uv**
- Сборка через **hatchling**
- Группы зависимостей (`dev`, `security`)
- Настроенный `pytest`
- Логирование через `loguru`
- Проверка уязвимостей через `pip-audit`
- Готовые каталоги `data/` и `logs/` с `.gitkeep`
- Чистый и минималистичный `pyproject.toml`

---

## 📁 Структура проекта

```
project/
│
├── src/
│   └── project_name/
│       ├── __init__.py
│       └── main.py
│
├── tests/
│   └── test_basic.py
│
├── data/
│   └── .gitkeep
│
├── logs/
│   └── .gitkeep
│
├── pyproject.toml
├── README.md
└── uv.lock
```

---

## 🛠 Установка зависимостей

```bash
uv sync
```

---

## ▶️ Запуск проекта

```bash
uv run src/project_name/main.py
```

---

## 🧪 Запуск тестов

```bash
uv run pytest
```

---

## 🔐 Проверка уязвимостей

```bash
uv run pip-audit
```

---

## 📦 Сборка wheel‑пакета

```bash
uv build
```

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.  
См. файл `LICENSE`.
