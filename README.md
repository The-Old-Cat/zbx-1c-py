# zbx-1c-py

Инструмент для интеграции 1С с Zabbix, позволяющий собирать информацию о базах данных 1С через сервер администрирования кластера (RAC) и передавать метрики в Zabbix для мониторинга производительности и состояния систем 1С.

Проект реализует схему Master/Dependent items для эффективного сбора метрик и поддерживает автоматическую масштабируемость: один вызов = один кластер.

---

## 🚀 Основные возможности

- Автоматическое обнаружение кластеров 1С (LLD - Low Level Discovery)
- Сбор метрик сессий и фоновых заданий для каждого кластера
- Проверка доступности сервиса RAS (Remote Administration Service)
- Поддержка аутентификации в кластере 1С
- Логирование через `loguru` с возможностью отладки
- Конфигурация через переменные окружения или файл `.env`

---

## 📁 Структура проекта

```
project/
│
├── src/
│   └── zbx_1c_py/
│       ├── __init__.py
│       ├── main.py
│       ├── config.py
│       ├── clusters.py
│       ├── session.py
│       ├── session_active.py
│       ├── background_jobs.py
│       ├── utils/
│       │   ├── helpers.py
│       │   └── check_config.py
│
├── tests/
│   └── test_basic.py
│
├── data/
│   └── .gitkeep
│
├── logs/
│   └── .gitkeep
│
├── scripts/
│   └── check_config.py
│
├── .env.example
├── pyproject.toml
├── README.md
└── LICENSE
```

---

## 🛠 Установка и настройка

### Требования

- Python 3.10+
- Утилита `rac` (Remote Administration Console) для 1С
- Доступ к серверу администрирования 1С (RAS)
- Учетные данные администратора кластера 1С

### Установка зависимостей

```bash
uv sync
```

### Конфигурация

Для работы приложения требуется настроить следующие параметры в файле `.env`:

- `RAC_PATH` — путь к утилите rac (обязательный параметр)
- `LOG_PATH` — путь к директории для логов (по умолчанию: "logs/")
- `USER_NAME` — имя пользователя администратора кластера 1С (обязательный параметр)
- `USER_PASS` — пароль пользователя администратора кластера 1С (обязательный параметр)
- `RAC_HOST` — хост сервера администрирования 1С (по умолчанию: "127.0.0.1")
- `RAC_PORT` — порт сервера администрирования 1С (по умолчанию: 1545)
- `DEBUG` — режим отладки (по умолчанию: false)

Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

Затем отредактируйте файл `.env`, указав свои значения:

```env
# Путь к утилите rac (различается в зависимости от ОС)
# Windows:
# RAC_PATH=C:/Program Files/1cv8/rac.exe
# Linux:
# RAC_PATH=/opt/1C/v8.3/x.x.x.x/rac
# macOS:
# RAC_PATH=/Applications/1C/Enterprise Platform/x.x.x.x/rac

# Хост и порт RAS-сервиса
RAC_HOST=127.0.0.1
RAC_PORT=1545

# Параметры аутентификации (опционально)
USER_NAME=your_username
USER_PASS=your_password

# Путь к директории для логов
LOG_PATH=./logs

# Режим отладки
DEBUG=false
```

---

## ▶️ Использование

### Режим обнаружения кластеров

Для получения JSON с информацией о кластерах 1С (для Zabbix LLD):

```bash
uv run zbx-1c-discovery --discovery
```

или

```bash
uv run src/zbx_1c_py/main.py --discovery
```

### Проверка доступности RAS

Для проверки доступности сервиса RAS:

```bash
uv run zbx-1c-check-ras --check-ras
```

или

```bash
uv run src/zbx_1c_py/main.py --check-ras
```

### Сбор метрик для конкретного кластера

Для сбора метрик для конкретного кластера 1С:

```bash
uv run zbx-1c-monitor <cluster_id>
```

или

```bash
uv run src/zbx_1c_py/main.py <cluster_id>
```

Пример:

```bash
uv run src/zbx_1c_py/main.py a1b2c3d4-5678-90ab-cdef-1234567890ab
```

### Сбор метрик для первого кластера (по умолчанию)

Если не указывать ID кластера, будут собраны метрики для первого доступного кластера:

```bash
uv run src/zbx_1c_py/main.py
```

### Проверка конфигурации

Для проверки корректности настройки конфигурации можно использовать вспомогательный скрипт:

```bash
# Рекомендуемый способ
uv run check-config

# Альтернативный способ
uv run python scripts/check_config.py
```

Скрипт проверяет:
- Наличие и доступность исполняемого файла rac
- Правильность настроек подключения к RAS
- Доступность директории для логов
- Правильность формата настроек
- Подключение к RAS

---

## 📊 Метрики, собираемые из 1С

Приложение собирает следующие метрики из кластеров 1С:

- Общее количество сессий
- Количество активных сессий (пользователи, реально работающие в системе)
- Количество активных фоновых заданий
- Статус кластера

Эти метрики возвращаются в формате JSON для дальнейшей обработки в Zabbix.

---

## 🧪 Запуск тестов

```bash
uv run pytest
```

---

## 🔐 Проверка уязвимостей

```bash
uv run python -m pip_audit
```

---

## 📦 Сборка проекта

```bash
uv build
```

---

## 🤝 Вклад в развитие проекта

Если вы хотите внести свой вклад в развитие проекта:

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Отправьте в репозиторий (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.
См. файл `LICENSE` для получения дополнительной информации.