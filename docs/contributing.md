# Вклад в проект (Contributing)

## Добро пожаловать!

Спасибо за интерес к проекту zbx-1c-py! Мы рады каждому вкладу, будь то исправление ошибок, добавление новой функциональности или улучшение документации. Проект кроссплатформенный и работает на Windows, Linux и macOS.

## Кодекс поведения

Участники проекта соглашаются соблюдать Кодекс поведения, который направлен на обеспечение уважительной и продуктивной среды для всех участников.

## Как внести вклад

### 1. Сообщения об ошибках

#### Перед созданием Issue:
- Проверьте, не было ли уже создано аналогичное сообщение
- Убедитесь, что вы используете последнюю версию проекта
- Подготовьте минимальный воспроизводимый пример ошибки

#### Формат Issue:
- Четкое и информативное название
- Подробное описание проблемы
- Шаги для воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Информация об окружении (версия Python, ОС, версия 1С)

### 2. Запросы на новые функции

#### Перед созданием Feature Request:
- Проверьте, не обсуждалась ли уже эта идея
- Подумайте, как новая функция вписывается в общую концепцию проекта
- Оцените сложность реализации
- Убедитесь, что новая функция поддерживает кроссплатформенность

#### Формат Feature Request:
- Четкое описание предлагаемой функции
- Обоснование необходимости функции
- Примеры использования
- Возможные варианты реализации
- Учет кроссплатформенных аспектов

### 3. Pull Requests

#### Перед созданием PR:
- Убедитесь, что вы работаете с последней версией ветки main
- Создайте отдельную ветку для вашего изменения
- Следуйте принятому стилю кодирования
- Добавьте тесты для нового функционала
- Обновите документацию при необходимости
- Убедитесь, что изменения поддерживают кроссплатформенность

#### Процесс PR:
1. Форкните репозиторий
2. Создайте новую ветку: `git checkout -b feature/amazing-feature`
3. Сделайте изменения
4. Добавьте тесты, если необходимо
5. Убедитесь, что все тесты проходят: `uv run pytest`
6. Зафиксируйте изменения: `git commit -m 'Add amazing feature'`
7. Отправьте ветку: `git push origin feature/amazing-feature`
8. Создайте Pull Request

## Стиль кодирования

### Python
- Следуйте PEP 8
- Используйте type hints для всех публичных функций
- Пишите docstrings в Google или NumPy формате
- Используйте осмысленные имена переменных и функций
- Поддерживайте читаемость кода

### Кроссплатформенные аспекты
- Учитывайте различия между операционными системами
- Используйте кроссплатформенные пути (os.path.join, pathlib)
- Обрабатывайте различия в кодировке
- Учитывайте различия в исполняемых файлах

### Пример оформления функции:
```python
def is_session_active(session: Dict[str, Any], threshold_minutes: int = 5) -> bool:
    """
    Проверяет, является ли сессия 1С активной.

    Args:
        session: Словарь с информацией о сессии
        threshold_minutes: Порог активности в минутах

    Returns:
        True, если сессия активна, иначе False
    """
    # Реализация функции
    pass
```

### Комментарии
- Пишите комментарии только там, где это действительно необходимо
- Объясняйте "почему", а не "что делает код"
- Обновляйте комментарии при изменении кода
- Указывайте кроссплатформенные особенности при необходимости

## Тестирование

### Требования к тестам:
- Все новые функции должны быть покрыты тестами
- Исправления ошибок должны включать тест, воспроизводящий ошибку
- Тесты должны быть независимыми и воспроизводимыми
- Используйте pytest для написания тестов
- Учитывайте кроссплатформенные аспекты при тестировании

### Структура тестов:
```
tests/
├── test_basic.py           # Базовые тесты
├── test_clusters.py        # Тесты модуля clusters
├── test_session.py         # Тесты модуля session
├── test_session_active.py  # Тесты модуля session_active
├── test_background_jobs.py # Тесты модуля background_jobs
└── conftest.py            # Общие фикстуры
```

## Документация

### Требования к документации:
- Обновляйте документацию при добавлении новой функциональности
- Используйте Markdown для оформления
- Пишите понятно и доступно
- Приводите примеры использования
- Учитывайте кроссплатформенные аспекты

### Структура документации:
```
docs/
├── index.md              # Главная страница документации
├── installation.md       # Установка и развертывание
├── configuration.md      # Настройка и конфигурация
├── api.md               # API и интерфейсы
├── architecture.md       # Архитектура проекта
├── zabbix-integration.md # Интеграция с Zabbix
├── testing.md           # Тестирование
├── security.md          # Безопасность
├── faq.md               # Часто задаваемые вопросы
└── contributing.md      # Как внести вклад (этот файл)
```

## Типы вклада

Мы приветствуем следующие типы вклада:

- 🐛 Исправления ошибок
- ✨ Новые функции
- 📚 Улучшения документации
- 🧪 Дополнительные тесты
- 🔧 Улучшения инструментов разработки
- 🚀 Оптимизация производительности
- 🌐 Локализация
- 💡 Идеи и предложения
- 🌐 Кроссплатформенные улучшения

## Процесс ревью

### Что проверяется:
- Соответствие стилю кодирования
- Покрытие тестами
- Качество документации
- Соответствие архитектуре проекта
- Потенциальные проблемы безопасности
- Кроссплатформенная совместимость

### Время ревью:
- Мы стремимся отвечать на Pull Requests в течение 1-2 недель
- Не стесняйтесь напоминать о вашем PR, если прошло больше времени

## Стиль коммитов

Используйте семантические сообщения коммитов:

```
feat: Добавить поддержку нового типа метрик
fix: Исправить ошибку в парсинге вывода rac
docs: Обновить документацию по установке
test: Добавить тесты для функции проверки активности
refactor: Переименовать переменные для лучшей читаемости
style: Исправить форматирование
perf: Оптимизировать алгоритм фильтрации
chore: Обновить зависимости
crossplat: Добавить поддержку кроссплатформенных путей
```

## Инструменты разработки

### Требования:
- Python 3.8+ (опционально, uv может управлять версиями Python)
- uv для управления зависимостями и изоляции среды
- git для контроля версий

### Установка для разработки:
```bash
# Клонирование репозитория
git clone https://github.com/your-repo/zbx-1c-py.git
cd zbx-1c-py

# Установка uv (если не установлен)
pip install uv

# Установка зависимостей в изолированное окружение
uv sync

# Установка pre-commit hooks (если настроены)
# pip install pre-commit && pre-commit install
```

### Команды для разработки:
```bash
# Запуск тестов (полностью изолированный от системного Python)
uv run pytest

# Запуск тестов с покрытием
uv run pytest --cov=src/zbx_1c_py

# Запуск конкретного теста
uv run pytest tests/test_specific.py

# Проверка безопасности
uv run pip-audit

# Запуск приложения
uv run src/zbx_1c_py/main.py --check-ras

# Запуск приложения с модулем Python
uv run python -m src.zbx_1c_py.main --discovery
```

### Управление средой разработки
Проект полностью изолирован от системного Python:
- Все зависимости устанавливаются через uv в виртуальное окружение
- Запуск всех команд осуществляется через `uv run`
- Версии пакетов фиксируются в файле `uv.lock`
- Для разработки не требуется установка Python в систему (uv может устанавливать нужную версию)

## Поддержка

Если у вас есть вопросы по внесению вклада:
- Создайте Issue с меткой "question"
- Обратитесь к документации
- Проверьте уже существующие Issue и Pull Requests

## Благодарности

Мы благодарим всех, кто вносит вклад в развитие проекта! Каждый вклад, большой или маленький, ценится и важен для нас.